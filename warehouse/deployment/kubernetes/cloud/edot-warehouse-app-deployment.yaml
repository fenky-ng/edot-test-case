apiVersion: apps/v1
kind: Deployment
metadata:
  name: edot-warehouse-app
  labels:
    app: edot-warehouse-app
spec:
  replicas: 3
  selector:
    matchLabels:
      app: edot-warehouse-app
  template:
    metadata:
      labels:
        app: edot-warehouse-app
    spec:
      containers:
        - name: edot-warehouse
          image: your-registry/edot-warehouse:latest
          ports:
            - containerPort: 9000
          env:
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: edot-warehouse-secret
                  key: DATABASE_URL
            - name: USER_REST_SERVICE_ADDRESS
              value: "http://edot-user-app:9100"
            - name: SHOP_REST_SERVICE_ADDRESS
              value: "http://edot-shop-app:9200"
            - name: PRODUCT_REST_SERVICE_ADDRESS
              value: "http://edot-product-app:9300"
            - name: API_KEY_EXT_ORDER
              valueFrom:
                secretKeyRef:
                  name: edot-warehouse-secret
                  key: API_KEY_EXT_ORDER
---
apiVersion: v1
kind: Service
metadata:
  name: edot-warehouse-app
spec:
  selector:
    app: edot-warehouse-app
  ports:
    - protocol: TCP
      port: 9400
      targetPort: 9000
  type: LoadBalancer
